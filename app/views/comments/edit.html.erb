<h1>Editing comment</h1>
 

<%= form_for([@article, @comment]) do |form| %>
 
 
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', articles_path %>